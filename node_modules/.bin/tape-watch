#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tape-watch/bin/tape-watch" "$@"
  ret=$?
else 
  node  "$basedir/../tape-watch/bin/tape-watch" "$@"
  ret=$?
fi
exit $ret
